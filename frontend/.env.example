# Copy this file to `.env` and adjust the values.
# With Create React App the variables MUST start with REACT_APP_.

# URL of the Laravel backend (no trailing /api).
REACT_APP_API_URL=http://localhost:8000

# Currency symbol used on the invoice screens. Company name, address,
# phone, email and logo are stored in the database and managed under
# Settings → Company.
REACT_APP_CURRENCY_SYMBOL=₹
